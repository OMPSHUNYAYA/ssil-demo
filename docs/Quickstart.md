# ⭐ Shunyaya Structural Irreversibility Layer (SSIL)

## **Quickstart — SSIL Deterministic Demo**

**Deterministic • Replay-Verifiable • Conservative Governance**  
**No Equation Modification • No Prediction • No Probabilistic Layer**

---

# 1. What This Repository Is

This repository is an executable SSIL observatory.

It allows you to:

- Execute the SSIL engine
- Observe finite irreversibility classification
- Verify deterministic replay identity
- Confirm conservative magnitude preservation
- Validate canonical conformance fingerprints

This repository is self-contained.

- No external services required  
- No internet required  
- No external dependencies required  

---

# 2. What SSIL Does — and Does Not Do

SSIL does **not**:

- Modify classical magnitude equations
- Modify thermodynamics or domain physics
- Predict collapse or failure
- Inject control logic
- Perform optimization
- Use machine learning
- Use probabilistic inference
- Adapt thresholds dynamically

SSIL overlays a deterministic irreversibility governance layer.

It:

- Classifies irreversibility posture deterministically
- Evaluates bounded structural recovery
- Detects irreversible boundary crossing (EdgeZero)
- Governs continuation admissibility
- Preserves magnitude exactly
- Produces replay-verifiable artifacts

---

# 3. Core Invariant (Non-Negotiable)

Conservative collapse mapping:

`phi((m,a,s,r)) = m`

Where:

- `m` = classical magnitude (unchanged)
- `a` = structural alignment
- `s` = accumulated posture
- `r ∈ {R0, E0, I1, I2, C}`

Magnitude is never altered.

---

# 4. Requirements

- Python 3.9+ (CPython recommended)
- Standard library only
- Offline-capable execution
- Deterministic execution discipline

No randomness.  
No statistical inference.  
No adaptive thresholds.

All verification must satisfy:

`B_A = B_B`

Byte-identical replay identity.

---

# 5. Repository Layout

```
ssil-demo/
│
├── LICENSE
├── README.md
├── RUN_ALL.bat
├── RUN_DEMO.bat
├── RUN_DEMO.sh
├── verify.py
│
├── docs/
│   ├── CASE_EDGEZERO_NOTE.md
│   ├── CASE_INDEX.md
│   ├── CASE_RECOVERY_WINDOW_NOTE.md
│   ├── FAQ.md
│   ├── PASS_EVIDENCE_CHECKLIST.md
│   ├── Quickstart.md
│   ├── SSIL-Conformance-Specification.md
│   ├── SSIL-Structural-Irreversibility-Model.md
│   ├── SSIL-Structural-Irreversibility-Topology-Diagram.png
│   ├── SSIL_DEMO_EXECUTION_SPEC.md
│   ├── SSIL_KILLER_DEMO_SPEC.md
│   └── SSIL_MIN_ADAPTER_SPEC.md
│
├── engine/
│   └── ssil_engine_v1_2.py
│
├── traces/
│   ├── trace_demo.csv
│   ├── trace_edgezero.csv
│   └── trace_recovery3.csv
│
├── reference_outputs/
│   ├── baseline_run1.csv
│   ├── baseline_run1.csv.sha256.txt
│   ├── baseline_run2.csv
│   ├── baseline_run2.csv.sha256.txt
│   ├── baseline_RUN_log.txt
│   ├── edgezero_run1.csv
│   ├── edgezero_run1.csv.sha256.txt
│   ├── edgezero_run2.csv
│   ├── edgezero_run2.csv.sha256.txt
│   ├── edgezero_RUN_log.txt
│   ├── recovery3_run.csv
│   ├── recovery3_run.csv.sha256
│   └── README.txt
│
├── OUT/
│   (Runtime demo outputs generated by RUN_DEMO / RUN_ALL)
│
├── OUT_VERIFY/
│   (Runtime verification outputs generated by verify.py)
│
└── VERIFY_CAPSULE/
    ├── BUILD_MANIFEST.bat
    ├── MANIFEST.sha256
    ├── RUN_VERIFY.bat
    │
    └── CAPSULE/
        ├── README.txt
        ├── RUN_ALL.bat
        ├── RUN_DEMO.bat
        ├── RUN_DEMO.sh
        │
        ├── docs/                (frozen capsule docs; may remain .txt)
        ├── engine/
        ├── reference_outputs/
        └── traces/
```

This structure is intentionally self-contained and replay-stable.

Important: `reference_outputs/` and `VERIFY_CAPSULE/` are **frozen evidence zones**. Do not rename or edit files inside them.

---

# 6. Cross-Platform Verification (Recommended)

From the repository root:

`python verify.py`

Expected final line:

`SSIL_VERIFY_RESULT: PASS`

The verifier enforces:

- Replay identity (`B_A = B_B`)
- Canonical fingerprint match
- Locked deterministic profile
- Deterministic artifact comparison
- Isolation from frozen reference outputs

Outputs are written to:

`OUT_VERIFY/`

If fingerprint mismatch occurs:

`SSIL_VERIFY_RESULT: FAIL`

No tolerance.  
No partial acceptance.  
Byte identity required.

---

# 7. Official Capsule Verification (Windows)

From repository root:

`VERIFY_CAPSULE\RUN_VERIFY.bat`

Expected final line:

`VERIFY_CAPSULE_RESULT: PASS`

The capsule verifies:

- Replay identity (`B_A = B_B`)
- Canonical fingerprint match
- Locked deterministic profile
- Environment normalization
- Deterministic profile immutability

If fingerprint mismatch occurs:

`VERIFY_CAPSULE_RESULT: FAIL`

No partial success is possible.

---

# 8. Demo Execution

## A. Windows (Batch)

Run canonical baseline demo:

`RUN_DEMO.bat baseline`

Run all deterministic demo cases:

`RUN_ALL.bat`

## B. macOS / Linux (Shell)

Run demo (if using the provided shell script):

`./RUN_DEMO.sh`

---

# 9. Manual Execution (Canonical Profile)

Run the canonical baseline trace:

```
python engine\ssil_engine_v1_2.py --in traces\trace_demo.csv --out manual_run.csv --H 8 --delta-max 0.15 --s-max 10.0 --rho 1.0 --beta 0.5 --gamma 0.5 --wA 0.6 --wB 0.2 --wS 0.2 --eta 0.25
```

To test replay identity, run twice:
```
python engine\ssil_engine_v1_2.py --in traces\trace_demo.csv --out run1.csv --H 8 --delta-max 0.15 --s-max 10.0 --rho 1.0 --beta 0.5 --gamma 0.5 --wA 0.6 --wB 0.2 --wS 0.2 --eta 0.25

python engine\ssil_engine_v1_2.py --in traces\trace_demo.csv --out run2.csv --H 8 --delta-max 0.15 --s-max 10.0 --rho 1.0 --beta 0.5 --gamma 0.5 --wA 0.6 --wB 0.2 --wS 0.2 --eta 0.25
```

Binary compare (Windows):

```
fc /b run1.csv run2.csv
```

Replay identity succeeds only if:

`no differences encountered`

Then verify SHA-256 (Windows):

`certutil -hashfile run1.csv SHA256`

---

# 10. Canonical Profile (Locked)

The canonical verification profile locks:

- `H = 8`
- `delta-max = 0.15`
- `s-max = 10.0`
- `rho = 1.0`
- `beta = 0.5`
- `gamma = 0.5`
- `wA = 0.6`
- `wB = 0.2`
- `wS = 0.2`
- `eta = 0.25`

Derived:

- `alpha = 1 / H`

Only this profile produces official conformance fingerprints.

All other parameter choices may remain deterministic, but are considered illustration profiles.

---

# 11. Structural Model Summary

Finite irreversibility regime set:

`R = {R0, E0, I1, I2, C}`

Bounded horizon:

`W_H(t) = {t-H+1, ..., t}`

Continuation mapping:

`IRR_ADM(t) ∈ {CONTINUE, ABSTAIN}`

Conservative collapse:

`phi((m,a,s,r)) = m`

SSIL governs continuation — not magnitude.

---

# 12. Deterministic Replay Rule

Two independent executions under identical inputs must produce:

`B_A = B_B`

Replay identity is authoritative proof of conformance.

---

# 13. What SSIL Is Not

SSIL does not:

- Replace thermodynamics
- Predict cascade failure
- Forecast market events
- Simulate mechanical breakdown
- Inject control signals
- Guarantee safety

It governs structural continuation admissibility only.

---

# 14. Final Summary

Shunyaya Structural Irreversibility Layer (SSIL):

- Introduces a deterministic finite irreversibility grammar
- Preserves classical magnitude via `phi((m,a,s,r)) = m`
- Governs continuation through `IRR_ADM(t)`
- Requires exact replay equivalence `B_A = B_B`
- Enforces canonical fingerprint verification

Magnitude remains primary.  
Irreversibility becomes finite.  
Continuation becomes governed.  
Verification becomes replayable.

